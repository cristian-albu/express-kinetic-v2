import { ContactContainer, ContactSec } from "@/sections/contact-section/styled";
import styled from "styled-components";

export const ThankYouSection = styled(ContactSec)`
    min-height: 100vh;
`;

export const ThankYouContainer = styled(ContactContainer)`
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
`;
